<body class="bg-cover bg-[url('/dark-vip-cinema-studio-still-life.jpg')] overflow-x-hidden">

  <img src="/result (1).png" class="w-3xs fixed left-60 mt-4">
  <div class="fixed right-0 mt-4 mr-45 inline-flex justify-center items-center py-1 px-3 text-base font-medium text-center text-white rounded-lg hover:bg-gray-200 hover:text-gray-800 border-4 border-gray-200">
    <%= button_to 'Back', search_path, method: :get, class: "cursor-pointer" %>
  </div>


  <div class="grid grid-cols-5 gap-4 mt-30">
    <% if @movies.present? %>
      <% @movies.each_with_index do |movie, index| %>
        <div class= "relative group">
          <img src="<%= movie["Poster"] %>" 
          alt="Poster for <%= movie["Title"] %>"
          id="movie-<%= index %>"
          data-popover-target="popover-<%= index %>"
          data-popover-trigger="hover"
          class="cursor-pointer"
          />
        
          <div data-popover 
            id="popover-<%= index %>"
            role="tooltip" 
            class="absolute z-10 invisible inline-block text-sm text-gray-500 transition-opacity duration-300 bg-white border border-gray-200 rounded-lg shadow-xs opacity-0 w-80 ">
              <div class="p-3">
                <div class="flex">
                  <div class="me-3 shrink-0">
                    <p class="font-semibold"><%= movie["Title"] %></p>
                    <p class="mt-2 text-xs"><%= movie["Year"] %></p>
                    <% if user_signed_in? %>
                      <div class="inline-flex items-center justify-center w-full px-5 py-2 me-2 text-sm mt-4 font-medium border rounded-lg focus:outline-none focus:z-10 focus:ring-4 focus:ring-gray-700 bg-gray-800 text-gray-400 border-gray-600 hover:text-white hover:bg-gray-700">
                        <%= link_to "Show", movie_path(movie["imdbID"]) %>
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
            <div data-popper-arrow></div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</body>